---
interface:
  display_name: "Task Retrospector"
  short_description: "Turns completed runs and failures into concrete Campfire improvements"
  default_prompt: "Use $task-retrospector and $task-handoff-state to analyze this completed or failed Campfire run and turn the reusable lessons into benchmark, verifier, skill, or control-plane follow-up candidates."

policy:
  allow_implicit_invocation: false
