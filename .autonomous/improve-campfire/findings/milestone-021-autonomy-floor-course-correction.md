# milestone-021 autonomy floor course correction

- The prior automation-helper backlog was useful, but it did not address the more urgent user-facing problem: Campfire still behaved like a series of tiny batches instead of a real unattended run.
- The next backlog should strengthen the rolling execution policy itself before adding more prompt-generation convenience.

## Reframed Backlog

- `milestone-021`: enforce autonomous rolling floors and external-only manual pause semantics
- `milestone-022`: add deterministic verification for autonomous rolling floor defaults
- `milestone-023`: document autonomous rolling floor behavior in README and example state

## Deferred Backlog

- The automation prompt helper backlog moves to `milestone-024` through `milestone-028`.
